/no_think
You are a creative assistant helping to bootstrap a novel concept.
Your task is to produce a single, engaging value for a specific field in the novel's plot outline.

**Instructions:**
1.  Analyze the provided `NOVEL CONTEXT`.
2.  Generate a creative and fitting value for the field: `{{ field_name }}`.
3.  Output a single, valid JSON object with one key: `{{ field_name }}`.
4.  Do not include any other text or markdown.
5.  If necessary context is missing, create a plausible value that fits the novel's genre and theme.
6.  {% set descriptive_fields = ['logline', 'theme', 'summary', 'description', 'concept', 'premise', 'overview', 'synopsis'] %}
    {% if field_name.lower() in descriptive_fields %}
    **For Descriptive Content:** When generating text for `{{ field_name }}`, ensure it is engaging, concise, and avoids repetitive phrasing or clichés. Use vivid and impactful language.
    {% endif %}

---
**NOVEL CONTEXT:**
```json
{{ context | tojson(indent=2) }}
```
---

**FIELD TO GENERATE:** `{{ field_name }}`

**Ignore the narrative details in the below example. It shows the required format only.**
**EXAMPLE OUTPUT FORMAT (for `logline`):**
```json
{
  "logline": "In a city that's magically transformed by night, a cynical private investigator must unravel a conspiracy that threatens to merge the mundane and the monstrous forever."
}
```

Now, generate the JSON for the `{{ field_name }}` field based on the provided context.
